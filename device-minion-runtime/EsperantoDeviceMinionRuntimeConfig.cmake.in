#------------------------------------------------------------------------------
# Copyright (C) 2019, Esperanto Technologies Inc.
# The copyright to the computer program(s) herein is the
# property of Esperanto Technologies, Inc. All Rights Reserved.
# The program(s) may be used and/or copied only with
# the written permission of Esperanto Technologies and
# in accordance with the terms and conditions stipulated in the
# agreement/contract under which the program(s) have been supplied.
#------------------------------------------------------------------------------

@PACKAGE_INIT@

set_and_check(ESPERANTO_DEVICE_MINION_RUNTIME_BIN_DIR "@PACKAGE_BIN_INSTALL_DIR@")
set_and_check(ESPERANTO_DEVICE_MINION_RUNTIME_INCLUDE_DIR "@PACKAGE_INCLUDE_INSTALL_DIR@")
set_and_check(ESPERANTO_DEVICE_MINION_RUNTIME_LIB_DIR "@PACKAGE_LIB_INSTALL_DIR@")

check_required_components(machine-minion)

get_filename_component(ESPERANTO_DEVICE_MINION_RUNTIME_CMAKE_DIR "${CMAKE_CURRENT_LIST_FILE}" PATH)

# Our library dependencies (contains definitions for IMPORTED targets)

if(NOT TARGET MachineMinion.elf)
  include("${ESPERANTO_DEVICE_MINION_RUNTIME_CMAKE_DIR}/EsperantoDeviceMinionRuntimeTargets.cmake")
endif()
